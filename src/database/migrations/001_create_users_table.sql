-- Enable RLS on auth.users (though it's already enabled by default)
-- This is just for documentation; Supabase handles auth.users automatically

-- Note: auth.users is managed by Supabase Auth
-- We cannot directly modify its structure, but we can set RLS policies if needed